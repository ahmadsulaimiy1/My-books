import SettingsView from './SettingsView';

export const metadata = {
  title: 'Settings — Preview | Albalagh Global',
  description: 'Frontend preview of the Albalagh Global Student Portal settings page, populated with sample data.',
  robots: { index: false, follow: false },
};

export default function SettingsPage() {
  return <SettingsView />;
}
